import { SxProps } from "@mui/material";

export const settingsBoxSx = {
    display: "flex",
    flexDirection: "column",
    flexGrow: 1
}

export const settingsButtonBoxSx: SxProps = {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    marginTop: "20px"
}